import random

def quickselect(data, n):
    return
def select(data, left, right, index):
    if right == left:
        return data[left]

    pivot_index = random.randint(left, right)




data = [2, 3, -2, 6, 8, 0]
print quickselect(data, 3)